// WebSocket is temporarily disabled. Use REST API hooks (e.g., useLiveETFData) instead.
export default function useETFWebSocket(symbol) {
  // Return null or a static value to avoid breaking dependent components
  return null;
} 